enum PriorityEnum {
  NONE
  LOW
  MEDIUM
  HIGH
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  completed: Boolean
  priority: PriorityEnum
  duedate: AWSDateTime
}
 